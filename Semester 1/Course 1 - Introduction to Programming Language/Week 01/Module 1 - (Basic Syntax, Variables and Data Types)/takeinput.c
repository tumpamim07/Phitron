#include<stdio.h>
int main()
{
    // int rahim,karim;
    // float f;
    // char c;
    // scanf("%d %f %c", &rahim, &f, &c);
    // printf("%d %0.2f %c", rahim, f, c);
    // return 0;


    //To take percentage we can follow 2 steps:
    //first one

    // int a,b;
    // char p;

    // scanf("%d%c %d%c",&a,&p,&b,&p);
    // printf("%d%c %d%c",a,p,b,p);
    // return 0;

    //second one
    int a,b;
    scanf("%d%% %d%%",&a,&b);
    printf("%d%% %d%%",a,b);
    return 0;
}